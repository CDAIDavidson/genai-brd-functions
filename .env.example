# ─────────── Emulator hosts (for local runs) ───────────
FIRESTORE_EMULATOR_HOST=127.0.0.1:8090
FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099
FIREBASE_STORAGE_EMULATOR_HOST=127.0.0.1:9199
STORAGE_EMULATOR_HOST=http://127.0.0.1:9199
PUBSUB_EMULATOR_HOST=127.0.0.1:8085

# ─────────── Project + runtime settings ───────────
GOOGLE_CLOUD_PROJECT=genai-brd-qi

# ─────────── Your business variables ───────────
DROP_FILE_BUCKET=brd-genai-sink                     # source GCS bucket
PROCESSED_BUCKET=brd-genai-doc-processor-input      # destination GCS bucket
METADATA_COLLECTION=metadata                        # Firestore collection
DOC_INDEX_TOPIC=document-indexer                    # Pub/Sub topic name
